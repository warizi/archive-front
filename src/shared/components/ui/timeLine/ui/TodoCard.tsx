import { Card, CardContent, CardHeader, CardTitle } from "../../card";

function TodoCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>
          할 일
        </CardTitle>
        <CardContent>

        </CardContent>
      </CardHeader>
    </Card>
  );
};

export default TodoCard;